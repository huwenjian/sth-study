# angular





# 安装 angular 最新版本
    rm -rf node_modules
    npm uninstall  --save-dev angular-cli
    npm install --save-dev @angular/cli@latest
    npm install


    npm config set registry  https://registry.npm.taobao.org


# 创建component
    首先使用ng g c config
# 创建模块和路由
    ng g m model --routing 创建模块和路由
    ng generate route my-route

